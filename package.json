{
  "name": "real-english-curriculum-prototype",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "REAL ENGLISH local course shell; not complete historical content",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node scripts/validate-data.mjs && node --check src/app.js && node --check scripts/dev-server.mjs && node --check scripts/import-chatgpt-export.mjs && node --test tests/*.test.mjs",
    "audit": "node scripts/validate-data.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
